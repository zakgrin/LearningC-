#include <stdio.h>
#include <stdlib.h>

int main(){

    // 1. Define Variables:
    // int:
    int x = 1;
    printf("%d\n", x); // %d refers to decimal number.
    x = 5; // when we change value we don't need to define type again.
    printf("%d\n", x);
}